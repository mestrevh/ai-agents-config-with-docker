# ai-agents-config-with-docker

Esse repositório possui formar de isolamento e execução dos agentes de IA para projetos pessoais

## Gemini CLI

Copie e cole o arquivo `docker-compose-gemini.yml` e execute o comando abaixo:

```bash
docker compose -f docker-compose-gemini.yml run --rm gemini
```